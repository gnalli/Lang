/** Sticky Header 占位高度，供 Hero 负 margin / 内边距与顶栏对齐 */
export const SITE_HEADER_OFFSET = {
  margin: "-mt-12 sm:-mt-14",
  padding: "pt-12 sm:pt-14",
  heroHeight: "h-[min(30rem,52vh)] sm:h-[min(34rem,56vh)]",
} as const
