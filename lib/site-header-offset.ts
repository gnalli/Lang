/** Sticky Header 占位高度，供 Hero 负 margin / 内边距与顶栏对齐（含导航边框） */
export const SITE_HEADER_OFFSET = {
  margin: "-mt-[3.75rem] sm:-mt-[4.5rem]",
  padding: "pt-[3.75rem] sm:pt-[4.5rem]",
  heroHeight: "h-[min(30rem,52vh)] sm:h-[min(34rem,56vh)]",
} as const
